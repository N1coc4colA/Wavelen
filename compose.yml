version: '3.8'

services:
  app:
    build: .
    ports:
      - "${HOST_PORT}:5000"        # use the host port from .env
    environment:
      - SECRET_KEY=${SECRET_KEY}   # pass secret into the container
    volumes:
      - ./data:/app/backend/data
    restart: unless-stopped
